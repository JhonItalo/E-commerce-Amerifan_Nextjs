export interface pokemonSmall {
     name: string;
     url: string;
}

export type pokemonInfo = {
     name: string;
     id: number;
     image: string;
     types: string[];
};

type storageType = {
     name: string;
     image: string;
     count: number;
};
